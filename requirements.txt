# Runtime dependencies for ollama_exporter.py.
# Pinned to a compatible-release range: patch/minor updates flow in,
# breaking majors are held back for reproducible, predictable builds.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
prometheus-client>=0.20,<1
httpx>=0.27,<1
